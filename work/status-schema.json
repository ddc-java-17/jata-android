Json Status / Long Poll Response Schema


{
    "key": "{game UUID key}"
    "boardSize": {int}  // REQUIRED WHEN JOINING/STARTING
    "playerCount": {int}  // REQUIRED WHEN JOINING/STARTING
    "boards": [
        {board schema},
        ...
    ]
}